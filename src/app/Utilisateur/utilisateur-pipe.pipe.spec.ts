import { UtilisateurPipePipe } from './utilisateur-pipe.pipe';

describe('UtilisateurPipePipe', () => {
  it('create an instance', () => {
    const pipe = new UtilisateurPipePipe();
    expect(pipe).toBeTruthy();
  });
});
